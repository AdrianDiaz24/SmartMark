<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/backend/src/database/db.js">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/backend/src/database/db.js" />
              <option name="originalContent" value="const sqlite3 = require('sqlite3').verbose();&#10;const { open } = require('sqlite');&#10;&#10;async function initDB() {&#10;&#10;    // 1. Abrimos la conexión&#10;    const db = await open({&#10;        filename: './src/database/smartmark.db',&#10;        driver: sqlite3.Database&#10;    });&#10;&#10;    console.log('Conexión a la base de datos SQLite establecida correctamente.');&#10;&#10;    // 2. Ejecutamos código SQL puro para crear todas las tablas&#10;    await db.exec(`&#10;        -- 1. TABLA DE CATEGORÍAS (Con soporte para subcarpetas y fecha de creación)&#10;        CREATE TABLE IF NOT EXISTS Categorias (&#10;            id INTEGER PRIMARY KEY AUTOINCREMENT,&#10;            nombre TEXT NOT NULL,&#10;            padre_id INTEGER, -- Si es NULL, va a la raíz. Si tiene un ID, es una subcarpeta.&#10;            fecha_creacion DATETIME DEFAULT CURRENT_TIMESTAMP,&#10;            FOREIGN KEY (padre_id) REFERENCES Categorias(id) ON DELETE CASCADE&#10;        );&#10;&#10;        -- 2. TABLA DE ETIQUETAS (Los Tags con color y fecha de creación)&#10;        CREATE TABLE IF NOT EXISTS Tags (&#10;            id INTEGER PRIMARY KEY AUTOINCREMENT,&#10;            nombre TEXT NOT NULL UNIQUE, -- El UNIQUE evita que guardemos &quot;#React&quot; dos veces&#10;            color TEXT NOT NULL, -- Almacena el color en formato hexadecimal (ej: #FF5733)&#10;            fecha_creacion DATETIME DEFAULT CURRENT_TIMESTAMP&#10;        );&#10;&#10;        -- 3. TABLA DE MARCADORES (Tus enlaces guardados)&#10;        CREATE TABLE IF NOT EXISTS Marcadores (&#10;            id INTEGER PRIMARY KEY AUTOINCREMENT,&#10;            titulo TEXT NOT NULL,&#10;            url TEXT NOT NULL,&#10;            descripcion TEXT,&#10;            portada BLOB, -- Almacena la imagen en formato binario&#10;            categoria_id INTEGER, -- Si es NULL, el enlace se muestra suelto en la raíz.&#10;            fecha_creacion DATETIME DEFAULT CURRENT_TIMESTAMP,&#10;            FOREIGN KEY (categoria_id) REFERENCES Categorias(id) ON DELETE CASCADE&#10;        );&#10;&#10;&#10;        -- 4. TABLA INTERMEDIA (Relación Muchos a Muchos)&#10;        -- Empareja un Marcador con un Tag&#10;        CREATE TABLE IF NOT EXISTS Marcadores_Tags (&#10;            marcador_id INTEGER,&#10;            tag_id INTEGER,&#10;            PRIMARY KEY (marcador_id, tag_id), -- Evita que le pongamos la misma etiqueta dos veces al mismo enlace&#10;            FOREIGN KEY (marcador_id) REFERENCES Marcadores(id) ON DELETE CASCADE,&#10;            FOREIGN KEY (tag_id) REFERENCES Tags(id) ON DELETE CASCADE&#10;        );&#10;    `);&#10;&#10;    console.log('Tablas creadas correctamente');&#10;&#10;    return db;&#10;}&#10;&#10;module.exports = initDB;&#10;" />
              <option name="updatedContent" value="const sqlite3 = require('sqlite3').verbose();&#10;const { open } = require('sqlite');&#10;&#10;async function initDB() {&#10;&#10;    // 1. Abrimos la conexión&#10;    const db = await open({&#10;        filename: './src/database/smartmark.db',&#10;        driver: sqlite3.Database&#10;    });&#10;&#10;    console.log('Conexión a la base de datos SQLite establecida correctamente.');&#10;&#10;    // 2. Ejecutamos código SQL puro para crear todas las tablas&#10;    await db.exec(`&#10;        -- 1. TABLA DE CATEGORÍAS (Con soporte para subcarpetas y fecha de creación)&#10;        CREATE TABLE IF NOT EXISTS Categorias (&#10;            id INTEGER PRIMARY KEY AUTOINCREMENT,&#10;            nombre TEXT NOT NULL,&#10;            padre_id INTEGER, -- Si es NULL, va a la raíz. Si tiene un ID, es una subcarpeta.&#10;            fecha_creacion DATETIME DEFAULT CURRENT_TIMESTAMP,&#10;            FOREIGN KEY (padre_id) REFERENCES Categorias(id) ON DELETE CASCADE&#10;        );&#10;&#10;        -- 2. TABLA DE ETIQUETAS (Los Tags con color y fecha de creación)&#10;        CREATE TABLE IF NOT EXISTS Tags (&#10;            id INTEGER PRIMARY KEY AUTOINCREMENT,&#10;            nombre TEXT NOT NULL UNIQUE, -- El UNIQUE evita que guardemos &quot;#React&quot; dos veces&#10;            color TEXT NOT NULL, -- Almacena el color en formato hexadecimal (ej: #FF5733)&#10;            fecha_creacion DATETIME DEFAULT CURRENT_TIMESTAMP&#10;        );&#10;&#10;        -- 3. TABLA DE MARCADORES (Tus enlaces guardados)&#10;        CREATE TABLE IF NOT EXISTS Marcadores (&#10;            id INTEGER PRIMARY KEY AUTOINCREMENT,&#10;            titulo TEXT NOT NULL,&#10;            url TEXT NOT NULL,&#10;            descripcion TEXT,&#10;            portada BLOB, -- Almacena la imagen en formato binario&#10;            categoria_id INTEGER, -- Si es NULL, el enlace se muestra suelto en la raíz.&#10;            fecha_creacion DATETIME DEFAULT CURRENT_TIMESTAMP,&#10;            FOREIGN KEY (categoria_id) REFERENCES Categorias(id) ON DELETE CASCADE&#10;        );&#10;&#10;        -- 4. TABLA INTERMEDIA (Relación Muchos a Muchos)&#10;        -- Empareja un Marcador con un Tag&#10;        CREATE TABLE IF NOT EXISTS Marcadores_Tags (&#10;            marcador_id INTEGER,&#10;            tag_id INTEGER,&#10;            PRIMARY KEY (marcador_id, tag_id), -- Evita que le pongamos la misma etiqueta dos veces al mismo enlace&#10;            FOREIGN KEY (marcador_id) REFERENCES Marcadores(id) ON DELETE CASCADE,&#10;            FOREIGN KEY (tag_id) REFERENCES Tags(id) ON DELETE CASCADE&#10;        );&#10;    `);&#10;&#10;    console.log('Tablas creadas correctamente');&#10;&#10;    return db;&#10;}&#10;&#10;module.exports = initDB;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>